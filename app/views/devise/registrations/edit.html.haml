.row.justify-content-center
  .col-md-6
    %h1
      Edit
      = resource_name.to_s.humanize
    = render "form", http_meth: :put
    = button_to "Cance my account", registration_path(resource_name), class: "btn btn-danger", data: { confirm: "Are you sure?" }, method: :delete
    = link_to "Back", :back